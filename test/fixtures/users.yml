# test/fixtures/users.yml

# We use ERB to generate a valid BCrypt hash for the password "password"
# This prevents the 'no column named password' error.

one:
  email_address: one@example.com
  password_digest: <%= BCrypt::Password.create('password') %>
  # Add other required fields if you have them (e.g., created_at: <%= Time.now %>)

two:
  email_address: two@example.com
  password_digest: <%= BCrypt::Password.create('password') %>